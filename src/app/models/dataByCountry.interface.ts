export interface dataByCountry{
    "Countries": [
        {
            Country: string;
            TotalConfirmed: number;
            TotalDeaths: number;
            TotalRecovered: number;
        }
    ];
}